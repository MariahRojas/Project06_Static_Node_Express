extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      //- Title 
      h1 My Portfolio!

      //- Description 
      p.lead.text-light
        | These are my projects! They showcase most of my skills in JavaScript as well as the libraries I've learned along the way!
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      each project, index in projects
        .cell
          a(href=`/project/${index}`)
            img.thumbnail(src=`${project.image_urls[0]}`)
            h5 #{project.project_name}
     
      //- Pug `each` iterator to creates a project img, title and link in the markup for each of my projects.  
      
      //- Interpolation and template literals to dynamically generate the the routes in the href, as well as the img's src and the projects title.


      
